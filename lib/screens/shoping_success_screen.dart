import 'package:flutter/material.dart';

class ShoppingSuccessScreen extends StatefulWidget {
  const ShoppingSuccessScreen({Key? key}) : super(key: key);

  @override
  _ShoppingSuccessScreenState createState() => _ShoppingSuccessScreenState();
}

class _ShoppingSuccessScreenState extends State<ShoppingSuccessScreen> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.start,
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [],
        ),
      ),
    );
  }
}
